**\[0:00 — 0:15] Introduction**"Hi, I’m Joshua, and this is ScriptForge — the AI content platform I built for the Blue Foxes assessment.I’ll quickly walk you through the app, the architecture, and the workflow automation behind it."\[0:15 — 1:00] Demo the App(Open the app)"So the idea behind this platform is simple — instead of a content team manually rewriting the same idea for multiple platforms, the system automates the whole process."Here The strategist enters a topic, selects the tone, the content length, and the platforms they want content for." I"The strategist enters a topic, selects the tone, the content length, and the platforms they want content for."**"In the background**, GPT-4o creates the first draft of the script, then Claude refines the tone and adapts it for each platform.Everything runs through a FastAPI backend.""So while this loads, the backend is orchestrating the AI workflow, validating outputs, and formatting content in parallel.""And here’s the result — a full YouTube script with a hook, sections, and outro, plus a LinkedIn version formatted differently for that platform.""That triggers an n8n workflow which handles automation like Slack notifications, Google Docs creation, and CMS scheduling automatically."=================================================================================================================================================================vvvvv**Git**The frontend is built with React and TypeScript&#x20;while the backend uses Python FastAPI."I tried to keep everything modular and easy to maintainSo services, routes, schemas, orchestrators, and automation workflows are separated into single-responsibility files.**CI/CD Pipeline**"You can see the CI/CD pipeline running automatically on every commit — linting the backend with ruff, type checking with mypy, and building the frontend. Every push is validated before it reaches production."=====================================================================================================================================================================\[0:15 — 1:00] Demo the App"Here's the backend running. You can see the logs in real time — every request coming in, which AI model is processing, token usage, and response times. This is what production observability looks like\[1:00 — 1:45] VS Code / Backend Architecture**(Open pipeline.py)**&#x20;"Here's the architecture. the orchestrator. "You can see the workflow is separated into stages.generation,refinement,validation,and formatting."(Scroll slowly)**openai\_service.py)**"I also added retry logic with exponential backoff for API reliability.So if an API rate limit happens temporarily, the system retries automatically instead of failing immediately."**claude\_service.py)**"And Claude is specifically responsible for tone refinement and formatting because I wanted each model handling the task it performs best at."´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´